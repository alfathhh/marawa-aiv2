[project]
name = "marawa-ai"
version = "0.4.0"
requires-python = ">=3.12"
dependencies = [
  "openpyxl>=3.1,<4",
  "psycopg[binary]>=3.2,<4",
  "qrcode[pil]>=8.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-ra"
